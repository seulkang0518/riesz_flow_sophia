logging:
  use_wandb: false

  log_every_k: 10

dataset:
  resolution: 256
  use_aug: false
  use_latent: true
  # The released cache contains 32x32 latents for 256px images, so encode the
  # 64px images online into 8x8 latents instead.
  use_cache: true
  num_classes: 1000
  # The local ILSVRC validation images are flat rather than class-organized.
  # Reuse the class-organized training split only for periodic sample previews.
  eval_split: train
  batch_size_per_gpu: 256
  eval_batch_size: 2048
  kwargs:
    num_workers: 8
    pin_memory: true
    prefetch_factor: 2

model:
  cond_dim: 768
  input_size: 32
  in_channels: 4
  patch_size: 2

  hidden_size: 768
  depth: 12
  num_heads: 12
  mlp_ratio: 4.0
  out_channels: 4

  use_qknorm: true
  use_swiglu: true
  use_rope: true
  use_rmsnorm: true
  n_cls_tokens: 16
  noise_classes: 64
  noise_coords: 32
  use_bf16: true
  attn_fp32: true

optimizer:
  lr_schedule:
    learning_rate: 0.0002
    warmup_steps: 5000
    lr_schedule: "const"
    total_steps: 100000
  weight_decay: 0.01
  adam_b1: 0.9
  adam_b2: 0.95

train:
  diverse_noise: true
  train_batch_size: 64
  seed: 42
  total_steps: 30000
  save_per_step: 2000
  keep_every: 10000
  eval_per_step: 5000
  eval_samples: 64
  sanity_samples: 64
  # A 64px preview has no matching reference-statistics file. Save sample
  # grids during evaluation without reporting a misleading ImageNet-256 FID.
  eval_fid: false
  eval_isc: false
  eval_prc_recall: false
  pos_per_sample: 64
  neg_per_sample: 16
  positive_bank_size: 128
  negative_bank_size: 1000
  forward_dict:
    gen_per_label: 64
    cfg_min: 1.0
    cfg_max: 4.0
    neg_cfg_pw: 3.0
  activation_kwargs:
    # At 8x8 latent resolution, retain the principal MAE spatial hierarchy and
    # avoid the 63 extra patch-statistic/intermediate-block feature views used
    # by the full 32x32 setup.
    patch_mean_size: [2, 4]
    patch_std_size: [2, 4]
    use_std: true
    use_mean: true
    every_k_block: 2
  loss_kwargs:
    R_list: [0.05]
  cfg_list: [1.0, 1.5, 2.0, 2.5, 3.0, 3.5]
  ema_decay: 0.999
  push_per_step: 64
  # Refill one normal batch after restoring a checkpoint. The old default of
  # 3000 batches looked like a freeze when latents were encoded online.
  push_at_resume: 1
  grad_accum_steps: 4
  max_grad_norm: 2.0

  ot_mode: "debiased"
  ot_kwargs:
    sinkhorn_num_iter: 10
    use_new_cfg: true
    disable_diag_mask: true
    resample_neg: true
    batch_sinkhorn: false
    use_quadratic_cost: true

    # Sinkhorn effective-neighbor diagnostics.
    # Every 100 steps, log how many positive samples the Sinkhorn coupling
    # effectively uses, for at most 2 feature views, to compare with top-k Riesz.
    sinkhorn_eff_diag_every: 100
    sinkhorn_eff_diag_max_features: 2

feature:
  mae_path: "hf://mae_latent_256"
  use_mae: true
  use_convnext: false
